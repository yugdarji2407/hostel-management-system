import {Outlet,useLocation} from 'react-router-dom';import {useState} from 'react';import TopHeader from '../components/TopHeader';import BottomNavigation from '../components/BottomNavigation';import SideDrawer from '../components/SideDrawer';export default function AppShell(){const [open,setOpen]=useState(false);useLocation();return <div className="app"><TopHeader onMenu={()=>setOpen(true)}/><main className="main"><Outlet/></main><BottomNavigation/>{open&&<SideDrawer onClose={()=>setOpen(false)}/>}</div>}
