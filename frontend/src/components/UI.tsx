import type {ReactNode} from 'react';import {AlertCircle,CheckCircle2,LoaderCircle} from 'lucide-react';export function PageHeader({title,subtitle}:{title:string;subtitle?:string}){return <div className="page-head"><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div>}export function StatusBadge({children}:{children:ReactNode}){const s=String(children).toLowerCase();let c=s.includes('reject')||s.includes('absent')||s.includes('blocked')?'red':s.includes('pending')||s.includes('due')||s.includes('review')?'orange':'green';return <span className={`badge ${c}`}>{children}</span>}export function EmptyState({title,text,icon=<AlertCircle size={42}/>}:{title:string;text:string;icon?:ReactNode}){return <div className="light-card empty">{icon}<h3>{title}</h3><p>{text}</p></div>}export function Loading(){return <div className="card" style={{textAlign:'center'}}><LoaderCircle className="spin"/><p className="muted">Loading...</p></div>}export function Modal({title,text,onClose,onConfirm}:{title:string;text:string;onClose:()=>void;onConfirm:()=>void}){return <div className="drawer-back"><div className="card" style={{position:'absolute',top:'50%',left:'50%',transform:'translate(-50%,-50%)',width:'min(430px,calc(100% - 32px))'}}><h2 style={{marginTop:0}}>{title}</h2><p className="muted">{text}</p><div className="actions" style={{justifyContent:'flex-end'}}><button className="btn secondary" onClick={onClose}>Cancel</button><button className="btn danger" onClick={onConfirm}>Confirm</button></div></div></div>}
