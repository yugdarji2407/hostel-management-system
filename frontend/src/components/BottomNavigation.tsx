import {NavLink} from 'react-router-dom';import {Home,PlusCircle,Ticket,QrCode,MessageSquare} from 'lucide-react';import {navItems} from '../lib/demo';const icons=[Home,PlusCircle,Ticket,QrCode,MessageSquare];export default function BottomNavigation(){return <nav className="bottom-nav">{navItems.map(([to,label],i)=>{const I=icons[i];return <NavLink key={to} to={to}><I/><span>{label}</span></NavLink>})}</nav>}
